{
  "name": "twitchcord-v2",
  "version": "0.0.1",
  "description": "The Twitchcord client modification for Discord",
  "repository": "https://gitlab.com/Twitchcord/twitchcord-v2",
  "author": "Twitchcord",
  "private": true,
  "scripts": {
    "build": "cd src && copyfiles **/*.json ../dist && cd .. && tsc",
    "setup": "npm install && cd src/modules/OldLoader/old-core && npm install && npm run setup"
  },
  "devDependencies": {
    "@types/node": "^11.10.5",
    "@types/react": "16.4.18",
    "@types/lodash": "^4.14.123",
    "copyfiles": "^2.1.0",
    "electron": "^4.0.7",
    "ts-node": "^8.0.3",
    "typescript": "^3.3.3333"
  }
}
